<UserControl x:Class="BeachBot.Desktop.Views.ChoosePartnerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:BeachBot.Desktop.ViewModels"
             xmlns:model="clr-namespace:BeachBot.Core.Models;assembly=BeachBot.Core"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:ChoosePartnerViewModel}">
    <UserControl.Resources>
        <!-- Search result: name + club + Select -->
        <DataTemplate x:Key="SearchResultTemplate" DataType="{x:Type model:Player}">
            <Border Style="{StaticResource Card}" Padding="12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding FullName}" FontFamily="Segoe UI Semibold" FontSize="14" />
                        <TextBlock Text="{Binding Club}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"
                                   Visibility="{Binding Club, Converter={StaticResource StringToVis}}" />
                    </StackPanel>
                    <Button Grid.Column="1" Content="Select" Style="{StaticResource AccentButton}" VerticalAlignment="Center"
                            Command="{Binding DataContext.SelectCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                            CommandParameter="{Binding}" />
                </Grid>
            </Border>
        </DataTemplate>

        <!-- Saved partner: name + club + Select + remove (x) -->
        <DataTemplate x:Key="SavedPartnerTemplate" DataType="{x:Type model:Player}">
            <Border Style="{StaticResource Card}" Padding="12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding FullName}" FontFamily="Segoe UI Semibold" FontSize="14" />
                        <TextBlock Text="{Binding Club}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"
                                   Visibility="{Binding Club, Converter={StaticResource StringToVis}}" />
                    </StackPanel>
                    <Button Grid.Column="1" Content="Select" Style="{StaticResource AccentButton}" VerticalAlignment="Center"
                            Command="{Binding DataContext.SelectCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                            CommandParameter="{Binding}" />
                    <Button Grid.Column="2" Content="✕" Style="{StaticResource IconButton}" VerticalAlignment="Center"
                            Margin="6,0,0,0" ToolTip="Remove from saved partners"
                            Command="{Binding DataContext.RemoveSavedCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                            CommandParameter="{Binding}" />
                </Grid>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <Button Content="← Back" Style="{StaticResource SubtleButton}" HorizontalAlignment="Left"
                    Padding="0,4" Command="{Binding BackCommand}" />
            <TextBlock Text="Choose a partner" Style="{StaticResource Heading}" Margin="0,6,0,0" />
            <TextBlock Style="{StaticResource Subheading}" Margin="0">
                <Run Text="for" /><Run Text=" " /><Run Text="{Binding TournamentName, Mode=OneWay}" FontWeight="SemiBold" />
            </TextBlock>
        </StackPanel>

        <!-- Search -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
            <Button Grid.Column="1" Content="Search" Style="{StaticResource AccentButton}" Margin="8,0,0,0"
                    IsDefault="True" Command="{Binding SearchCommand}" />
        </Grid>

        <!-- Lists -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Saved partners -->
                <StackPanel Visibility="{Binding HasSavedPartners, Converter={StaticResource BoolToVis}}">
                    <TextBlock Text="SAVED PARTNERS" Style="{StaticResource SectionTitle}" />
                    <ItemsControl ItemsSource="{Binding SavedPartners}" ItemTemplate="{StaticResource SavedPartnerTemplate}" Margin="0,0,0,14" />
                </StackPanel>

                <!-- Search results -->
                <TextBlock Text="SEARCH RESULTS" Style="{StaticResource SectionTitle}" />
                <ItemsControl ItemsSource="{Binding SearchResults}" ItemTemplate="{StaticResource SearchResultTemplate}" />
                <TextBlock Text="Type a name and press Search." Foreground="{StaticResource MutedBrush}" Margin="2,2,0,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SearchResults.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}" Margin="2,10,0,0"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVis}}" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
