<UserControl x:Class="BeachBot.Desktop.Views.MainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:BeachBot.Desktop.ViewModels"
             xmlns:model="clr-namespace:BeachBot.Core.Models;assembly=BeachBot.Core"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:MainMenuViewModel}">
    <UserControl.Resources>
        <!-- Status pill colouring -->
        <Style x:Key="StatusText" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="Scheduled">
                    <Setter Property="Foreground" Value="{StaticResource AccentHoverBrush}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Submitting">
                    <Setter Property="Foreground" Value="{StaticResource WarningBrush}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Succeeded">
                    <Setter Property="Foreground" Value="{StaticResource SuccessBrush}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Failed">
                    <Setter Property="Foreground" Value="{StaticResource DangerBrush}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- A registration row -->
        <DataTemplate x:Key="RegistrationTemplate" DataType="{x:Type model:Registration}">
            <Border Style="{StaticResource Card}" Padding="14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="{Binding TournamentName}" FontFamily="Segoe UI Semibold" FontSize="15" />
                        <TextBlock Foreground="{StaticResource TextSecondaryBrush}" Margin="0,3,0,0">
                            <Run Text="Partner:" />
                            <Run Text="{Binding PartnerName}" />
                        </TextBlock>
                        <TextBlock Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,0">
                            <Run Text="Opens:" />
                            <Run Text="{Binding RegistrationOpensAt, Converter={StaticResource LocalDateTime}, Mode=OneWay}" />
                        </TextBlock>
                        <TextBlock Text="{Binding ResultMessage}" Margin="0,4,0,0"
                                   Foreground="{StaticResource MutedBrush}" FontSize="12"
                                   Visibility="{Binding ResultMessage, Converter={StaticResource StringToVis}}" />
                    </StackPanel>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right">
                        <TextBlock Text="{Binding Status}" Style="{StaticResource StatusText}" HorizontalAlignment="Right" />
                        <Button Content="Cancel" Style="{StaticResource SubtleButton}" Margin="0,8,0,0"
                                Visibility="{Binding IsPending, Converter={StaticResource BoolToVis}}"
                                Command="{Binding DataContext.CancelCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                CommandParameter="{Binding}" />
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Grid Grid.Row="0" Margin="0,0,0,18">
            <StackPanel>
                <TextBlock Text="Your registrations" Style="{StaticResource Heading}" />
                <TextBlock Text="Scheduled registrations fire automatically when the tournament opens."
                           Style="{StaticResource Subheading}" Margin="0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                <Button Content="+ New registration" Style="{StaticResource AccentButton}"
                        Command="{Binding NewRegistrationCommand}" />
                <Button Content="Log out" Style="{StaticResource SubtleButton}" Margin="8,0,0,0"
                        Command="{Binding LogoutCommand}" />
            </StackPanel>
        </Grid>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Scheduled -->
                <TextBlock Text="SCHEDULED" Style="{StaticResource SectionTitle}" />
                <ItemsControl ItemsSource="{Binding Scheduled}" ItemTemplate="{StaticResource RegistrationTemplate}" />
                <TextBlock Text="No scheduled registrations yet. Click “New registration” to add one."
                           Foreground="{StaticResource MutedBrush}" Margin="2,0,0,4">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasScheduled}" Value="False">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- Past -->
                <TextBlock Text="HISTORY" Style="{StaticResource SectionTitle}" Margin="2,22,0,6" />
                <ItemsControl ItemsSource="{Binding Past}" ItemTemplate="{StaticResource RegistrationTemplate}" />
                <TextBlock Text="Nothing here yet." Foreground="{StaticResource MutedBrush}" Margin="2,0,0,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasPast}" Value="False">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
