<UserControl x:Class="BeachBot.Desktop.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:BeachBot.Desktop.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:LoginViewModel}">
    <Grid>
        <Border Style="{StaticResource Card}" Width="380" VerticalAlignment="Center" HorizontalAlignment="Center"
                Padding="28">
            <StackPanel>
                <TextBlock Text="Sign in" Style="{StaticResource Heading}" />
                <TextBlock Text="Log in to beachvolleyball.de to schedule registrations."
                           Style="{StaticResource Subheading}" />

                <TextBlock Text="Email" Style="{StaticResource SectionTitle}" Margin="0,8,0,6" />
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="Password" Style="{StaticResource SectionTitle}" Margin="0,14,0,6" />
                <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_OnPasswordChanged" />

                <CheckBox Content="Remember me" IsChecked="{Binding RememberMe}" Margin="0,16,0,0" />

                <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}"
                           Margin="0,14,0,0" Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVis}}" />

                <Button Content="Sign in" Style="{StaticResource AccentButton}" IsDefault="True"
                        Command="{Binding LoginCommand}" Margin="0,18,0,0" HorizontalAlignment="Stretch" />

                <TextBlock Text="Connecting…" Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Center" Margin="0,12,0,0"
                           Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
